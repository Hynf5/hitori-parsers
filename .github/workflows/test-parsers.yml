name: Parsers check

on:
  push:
    branches:
      - hitori-pr
    paths:
      - 'src/main/kotlin/org/koitharu/kotatsu/parsers/**'

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository 🌏
        uses: actions/checkout@v5

      - name: Set up Java 🔧
        uses: actions/setup-java@v5
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Set up Gradle 📦
        uses: gradle/actions/setup-gradle@v4

      - name: Compile parsers 🚀
        id: build
        run: ./gradlew compileKotlin

      # ===============================
      # ✅ SUCCESS NOTIFICATION
      # ===============================
      - name: Notify Discord (Success)
        if: success()
        uses: tsickert/discord-webhook@v7.0.0
        with:
          webhook-url: ${{ secrets.DISCORD_WEBHOOK_URL }}
          embed-title: "✅ Parsers Build Success"
          embed-description: |
            📁 Repo: `${{ github.repository }}`
            🌿 Branch: `${{ github.ref_name }}`
            📝 Commit:
            > ${{ github.event.head_commit.message }}
          embed-color: 3066993
          embed-url: https://github.com/${{ github.repository }}/commit/${{ github.sha }}
          embed-footer-text: ${{ github.actor }}
          embed-footer-icon-url: https://github.com/${{ github.actor }}.png

      # ===============================
      # ❌ ERROR NOTIFICATION
      # ===============================
      - name: Notify Discord (Failure)
        if: failure()
        uses: tsickert/discord-webhook@v7.0.0
        with:
          webhook-url: ${{ secrets.DISCORD_WEBHOOK_URL }}
          embed-title: "❌ Parsers Build Failed"
          embed-description: |
            🚨 **Error Summary**
            • Job: `build-and-test`
            • Step: `Compile parsers`
            
            📝 Commit:
            > ${{ github.event.head_commit.message }}

            🔗 Logs:
            https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
          embed-color: 15158332
          embed-footer-text: ${{ github.actor }}
          embed-footer-icon-url: https://github.com/${{ github.actor }}.png
